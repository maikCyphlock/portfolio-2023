/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['media.architecturaldigest.com','visionnoventa.com','user-images.githubusercontent.com'],
    
  },
}

module.exports = nextConfig
